# file to fetch the properties from github
# application name where we fetch the properties
spring.application.name=the-school
# using discovery client to locate the config server
spring.cloud.config.discovery.enabled=true
# use to find the config server by id
spring.cloud.config.discovery.service-id=config-server
eureka.client.server-url.defaultZone=http://localhost:8761/eureka